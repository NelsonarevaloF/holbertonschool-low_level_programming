#include <stdio.h>

/**
 * main - this program prints a message
 * Description: it is printed through the printf function
 * Return: always return 0
 */

int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
